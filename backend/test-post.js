const axios = require('axios');

async function test() {
  const rootCardId = 11;
  const baseUrl = 'http://localhost:5001/api';
  
  // You might need an auth token if it's protected
  // For this test, I'll assume I can run it if I bypass auth or if I have a token
  // Since I can't easily get a token, I'll just check the code again.
}
